#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always return o
 */
int main(void)
{
	char p;

	int c;

	p = 'a';
	c = 0;

	while
		(c < 10)
		{
			putchar(c + '0');
			c++;
		}
	while
		(p <= 'f')
		{
			putchar(p);
			p++;
		}
			putchar('\n');
			return (0);
}
