#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always return o
 */
int main(void)
{
	char p;

	p = 'z';

	while
		(p >= 'a')
		{
			putchar(p);
			p--;
		}
			putchar('\n');
			return (0);
}
